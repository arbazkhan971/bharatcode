export const meta = {
  name: 'bharatcode-docs',
  description: 'Build an end-to-end /docs section on the Next.js site: docs layout with sidebar nav + content pages, then build-verify',
  phases: [{ title: 'DocsLayout' }, { title: 'DocsPages' }, { title: 'Integrate' }],
}

const ROOT = '/Users/arbaz/bharatcode'
const WEB = `${ROOT}/web`

const FACTS = `BharatCode is a Go-native MIT-licensed open-weight-first CLI coding agent ("OpenCode for India" — data stays in India). REAL, VERIFIED facts to document (do NOT invent beyond these):
- Install: go install github.com/arbazkhan971/bharatcode@latest (Go 1.25+, CGO-free). Repo: github.com/arbazkhan971/bharatcode. Domain: bharatcode.dev.
- Run: \`bharatcode\` launches the TUI. \`bharatcode run "<prompt>"\` is headless; \`bharatcode run --json\` emits NDJSON events; \`--output-last-message <file>\` writes the final message. \`bharatcode --continue\` resumes the latest session.
- CLI subcommands: run, login, logout, models, sessions, stats, budget, update-providers, config, version, doctor.
- 13 built-in tools: view, edit, multiedit, write, ls, glob, grep, bash, web_fetch, web_search, diagnostics, todo, plus job_output/job_kill (background job control).
- Slash commands (in the TUI): /help, /clear, /model, /agent, /sessions (restore), /fork (branch session), /diff, /status, /goal (set/run/clear/stop — autonomous bounded iteration), /permissions (read-only|auto|full approval modes), /budget, /yolo, /save, /quit, and custom /<name> prompts from a Markdown prompt registry (~/.bharatcode/prompts/*.md with {{input}}/{{var}} interpolation).
- Providers (config-driven): DeepSeek, Moonshot/Kimi, Groq, Together, Fireworks, OpenRouter, Google Gemini, OpenAI, Anthropic (open-weight ones first-class), plus local Ollama and LM Studio. Each is a provider entry in config with name/type/base_url/api_key_env/models; types are anthropic, openai, openai_compatible, ollama, lmstudio. Keys via env vars (DEEPSEEK_API_KEY, MOONSHOT_API_KEY, GROQ_API_KEY, etc.).
- Config: global ~/.config/bharatcode/config.json merged with project ./.bharatcode.json; --profile <name> overlays a named config; AGENTS.md/CLAUDE.md project instructions are ingested into the system prompt (nested, root-first override, byte-capped).
- Integrations: MCP client (stdio/HTTP/SSE transports — configure servers in config); LSP integration (in-context diagnostics); shell-backed lifecycle hooks (PreToolUse/PostToolUse/OnError/OnSession with glob/regex match, JSON payload, block decisions).
- Permissions: ask/allow/deny with once/session/project/forever scopes; approval modes read-only/auto/full; --yolo bypass; INR-aware cost ledger with a monthly budget gate.
- HONESTY: no fabricated benchmarks/specs. NEVER mention "Crush", "Charm" as a company, "FSL", or "clean-room" (charmbracelet/bubbletea etc. as tech credits is fine).`

const COMMON = `You are building the /docs section of the bharatcode.dev website (Next.js App Router + TypeScript + Tailwind, in ${WEB}). The marketing site and its design system (app/components/ui/: Section, Button, Badge, CodeBlock, Terminal) already exist and build with \`next build\` (static export: output 'export'). Reuse those UI components and the existing Tailwind tokens/dark theme for visual consistency. Mobile-responsive, accessible. ${FACTS}\n\nOnly create/edit your assigned files. Write clean React/TS. Use 'use client' only where needed (interactivity). Keep MDX-free: write docs as TSX content components (plain JSX prose with the shared CodeBlock for code), so no extra MDX tooling/deps are required.`

const S = { type:'object', properties:{ part:{type:'string'}, filesCreated:{type:'array',items:{type:'string'}}, notes:{type:'string'} }, required:['part','filesCreated'] }

// PHASE 1: docs layout + sidebar nav + shared docs primitives. Must finish first.
phase('DocsLayout')
const layout = await agent(`${COMMON}\n\nTASK: Build the /docs shell. Create:\n- ${WEB}/app/docs/layout.tsx — a docs layout with a LEFT SIDEBAR navigation (grouped: "Getting Started" [Introduction, Installation, Quick Start], "Configuration" [Config files, Providers & Models, Profiles, AGENTS.md], "Usage" [TUI & Slash Commands, Built-in Tools, /goal Autonomous Mode, Sessions & Fork, Permissions], "Integrations" [MCP, LSP, Hooks], "Reference" [CLI Reference]) and a main content area. Sidebar links point to /docs, /docs/installation, /docs/quick-start, /docs/configuration, /docs/providers, /docs/profiles, /docs/agents-md, /docs/commands, /docs/tools, /docs/goal, /docs/sessions, /docs/permissions, /docs/mcp, /docs/lsp, /docs/hooks, /docs/cli. Highlight the active route. Responsive: sidebar collapses to a top nav / drawer on mobile.\n- ${WEB}/app/docs/components/DocsPage.tsx — a reusable content wrapper (title, optional subtitle/lede, prev/next links, consistent prose styling using Tailwind 'prose'-like classes or hand-styled headings/p/ul/code). Export helpers like DocsH2, DocsP, DocsList, InlineCode if useful so page agents write consistent prose.\n- ${WEB}/app/docs/page.tsx — the Introduction page (what BharatCode is, the three pillars, who it's for, a 'next: Installation' link). Real content from the FACTS.\nReturn the file list and the exact route list so page agents match it.`, { label:'docs-layout', phase:'DocsLayout', schema:S })
log(`Docs layout done: ${(layout?.filesCreated||[]).length} files`)

// PHASE 2: content pages in parallel — each owns ONE route file.
phase('DocsPages')
const page = (route, title, brief) => () => agent(`${COMMON}\n\nThe docs layout + DocsPage wrapper exist (import from ../components/DocsPage or app/docs/components/DocsPage). TASK: create ONE docs page file at ${WEB}/app/docs/${route}/page.tsx exporting a default component. TITLE: "${title}". CONTENT: ${brief}\n\nWrite thorough, accurate, well-structured docs prose using the shared DocsPage wrapper and CodeBlock for all commands/config. Include real, copy-pasteable examples. Ground everything in the FACTS — do not invent flags, fields, or model names not listed. Add a prev/next link if the wrapper supports it. Return the file path.`, { label:`docs:${route}`, phase:'DocsPages', schema:S })

const pages = await parallel([
  page('installation', 'Installation', 'How to install: go install github.com/arbazkhan971/bharatcode@latest; Go 1.25+ requirement, CGO-free note; verifying with `bharatcode version` and `bharatcode doctor`; building from source. Note macOS/Linux support.'),
  page('quick-start', 'Quick Start', 'First run: set a provider key (e.g. export DEEPSEEK_API_KEY=...), run `bharatcode`, type a prompt. A fully-local example with Ollama (no key, data stays local). Headless: `bharatcode run "fix the failing test"`, and `bharatcode run --json` for CI. Resuming with --continue. A short tour of the TUI (chat, footer with INR cost, slash commands).'),
  page('configuration', 'Configuration', 'Config files: global ~/.config/bharatcode/config.json merged with project ./.bharatcode.json (project overrides global). Show a full annotated config.json example (providers array, models, ledger budget, permissions). Explain `bharatcode config` and where the data dir lives.'),
  page('providers', 'Providers & Models', 'How providers work: each is a config entry with name/type/base_url/api_key_env/models. Provider types: anthropic, openai, openai_compatible, ollama, lmstudio. Table of supported providers + their env var, marking open-weight + local ones (DeepSeek, Moonshot/Kimi, Groq, Together, Fireworks, OpenRouter, Google Gemini, Ollama-local, LM Studio-local, OpenAI, Anthropic). How to add a custom OpenAI-compatible endpoint. `bharatcode models` to list, `bharatcode update-providers`. Emphasize local/India-hosted = data stays in India.'),
  page('profiles', 'Profiles', 'Named config overlays: --profile <name> overlays ~/.config/bharatcode/<name>.json on top of the merged config (profile wins). Use cases: a locked-down review profile vs a full-access scripting profile. Example files + invocation.'),
  page('agents-md', 'Project Instructions (AGENTS.md)', 'AGENTS.md / CLAUDE.md ingestion: files are loaded from a chain (global then repo-root down to cwd), concatenated root-first so nested files override, byte-capped, and injected into the system prompt so the agent follows house conventions. Example AGENTS.md and how nesting/override works.'),
  page('commands', 'TUI & Slash Commands', 'The interactive TUI and its slash commands. Document each: /help, /clear, /model, /agent, /sessions (restore a past session), /fork (branch the current session), /diff (show recent edit diffs), /status, /goal (set/run/clear/stop), /permissions (read-only|auto|full), /budget, /yolo, /save, /quit. Plus custom Markdown prompts: drop a file in ~/.bharatcode/prompts/<name>.md with {{input}}/{{var}} placeholders and invoke it as /<name> <args>. Give an example custom prompt.'),
  page('tools', 'Built-in Tools', 'The 13 built-in tools the agent can call: view, edit, multiedit, write (file ops), ls, glob, grep (search), bash (shell, permission-gated, supports background jobs via job_output/job_kill), web_fetch, web_search (web), diagnostics (LSP), todo (task list). For each: a one-line description and when the agent uses it. Note that bash and write/edit are permission-gated.'),
  page('goal', 'Autonomous /goal Mode', 'How /goal drives bounded autonomous iteration: /goal <text> sets a goal, /goal run starts a run->observe->continue loop until the goal is met or an iteration cap is hit, /goal stop halts it, /goal clear resets. Explain the iteration cap and how progress surfaces in the TUI. Best-practice tips for writing good goals.'),
  page('sessions', 'Sessions & Fork', 'Persistent sessions: SQLite-backed, every conversation is saved. Resume the latest with `bharatcode --continue`; pick a past one with /sessions; branch an exploration with /fork (creates a new session copying history, leaving the original untouched). `bharatcode sessions` to list. Where the DB lives.'),
  page('permissions', 'Permissions & Cost', 'The permission model: tool calls are gated — ask/allow/deny with once/session/project/forever scopes. Approval modes via /permissions: read-only (only read tools auto-run), auto (default, ask per scope), full (allow all, like --yolo). Config deny-list/auto-approve patterns (exact match or <tool>:* wildcard). The INR cost ledger: per-call rupee accounting, a monthly budget gate (`bharatcode budget`), and the TUI footer showing spend.'),
  page('mcp', 'MCP Integration', 'Model Context Protocol: BharatCode is an MCP client supporting stdio, HTTP, and SSE transports. How to configure MCP servers in config (a servers map with command/args/env, or url for remote), how bridged tools appear to the agent (server-prefixed names), and per-server permission. A worked example (e.g. a filesystem or GitHub MCP server).'),
  page('lsp', 'LSP Integration', 'Language Server Protocol: BharatCode runs language servers to give the agent in-context diagnostics (the diagnostics tool). How to configure a language server per language in config. Note it handles server-initiated requests correctly (works with real gopls/rust-analyzer).'),
  page('hooks', 'Hooks', 'Shell-backed lifecycle hooks: run commands on PreToolUse, PostToolUse, OnError, OnSession events, matched by glob/regex against the tool. Hooks receive a JSON payload on stdin (event, tool, args, session_id) and BHARATCODE_* env vars; a PreToolUse hook can return a block decision to veto a tool call. Config example + a use case (e.g. block `rm -rf`, run gofmt after edits).'),
  page('cli', 'CLI Reference', 'Full command reference: bharatcode (TUI), run [--json --output-last-message --continue], login/logout, models, sessions, stats, budget, update-providers, config, doctor, version. For each: synopsis, key flags, and a one-line example. Format as a clean reference.'),
])
const pClean = pages.filter(Boolean)
log(`Docs pages done: ${pClean.length} pages`)

// PHASE 3: build-verify the whole site (docs + marketing).
phase('Integrate')
const IG = { type:'object', properties:{ buildPasses:{type:'boolean'}, routesBuilt:{type:'array',items:{type:'string'}}, fixesApplied:{type:'array',items:{type:'string'}}, crushResidual:{type:'number'}, notes:{type:'string'} }, required:['buildPasses'] }
const integ = await agent(`You are the integration gate for the bharatcode.dev site after a /docs section was added (a docs layout with sidebar + ~14 content pages under ${WEB}/app/docs/).\n\nDO:\n1. Ensure all docs routes resolve and imports are correct (DocsPage wrapper path, shared UI imports, 'use client' where interactive). Fix mismatches.\n2. Add a "Docs" link in the site header/nav (and the Footer if present) pointing to /docs, so docs are reachable from the marketing pages — read how the existing nav/header is structured first and edit minimally.\n3. cd ${WEB} && npm run build — fix every error until the static export SUCCEEDS (type errors, missing 'use client', invalid links, static-export incompatibilities). Re-run until green.\n4. cd ${WEB} && grep -rin 'crush\\\\|FSL\\\\|clean-room' app/ --include=*.tsx --include=*.ts must be 0 (charmbracelet credit fine).\n5. Report: build pass/fail, the list of /docs routes that built, fixes applied, crush residual.\nBar: \`npm run build\` succeeds with all docs routes statically exported, docs reachable from the main nav, zero forbidden terms.`, { label:'integrate-docs', phase:'Integrate', schema:IG })

return { layout, pages: pClean, integration: integ }
